int[] a = [1,2,3];

printi(a[0]);
printi(a[1]);
printi(a[2]);
int z = 1;
printi(a[z]);

char[] b = ['a','b','c'];

printc(b[0]);
printc(b[1]);
printc(b[2]);

float[] f = [1.0, 3.4, 2.1];
printf(f[0]);
printf(f[1]);

int[] copy = a;
printi(copy[0]);

char[] ccopy = b;
printc(ccopy[2]);

int foo() {
    int[] d = [1,2,3];
    int y = 4;
    int[] x = d;
    return x[2];
}

float bar() {
    float[] fl = [0.1, 2.3, 4.5];
    float[] flo = fl;
    return flo[2];
}

int[] baz() {
    int[] ret = [1,2,3];
    return ret;
}

printi(foo());
printf(bar());